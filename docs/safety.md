# Safety

- Live execution is OFF by default.
- Any external side effects should require explicit operator approval.
- Secrets must remain in local env files / secure stores (never in repo).
- Confidence-tier fallback mode must be active for degraded feeds.
